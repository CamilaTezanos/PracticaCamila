let uno = prompt("primer numero");
parsenInt(uno);
let dos = prompt("segundo numero");
parsenInt(dos);
if (dos);
{
    tres = uno/dos
    document.writeln("El resultado de la division es " + tercero);
}
else if (dos == 0)

{
    document.writeln("no es divisible entre 0");
}
